import { StyleSheet, View } from 'react-native'
import React from 'react'

// LOCAL IMPORTS
import AppText from '../../AppText'

const Albums = () => {
  return (
    <View>
      <AppText>Albums</AppText>
    </View>
  )
}

export default Albums

const styles = StyleSheet.create({})